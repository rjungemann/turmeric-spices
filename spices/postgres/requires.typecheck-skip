postgres: type-check failures pending fix.

  src/postgres/db.tur, stmt.tur: `if` condition is :int but must be :bool
                                  (type-system tightening was not propagated
                                  to these comparisons).
  src/postgres/notify.tur: __pq-exec-raw expects :cstr but is called with :int.

Remove this marker once `tur check src/postgres/*.tur` exits 0 for every file.
