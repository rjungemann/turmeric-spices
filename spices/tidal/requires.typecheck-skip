tidal: type-check failures pending fix (all 4 source files affected).

  src/tidal/event.tur, notation.tur, pattern.tur: local `(defn ok ...)`
        shadows stdlib's auto-loaded result.tur `ok`. Rename the local
        definitions (e.g. `tidal-ok`, or wrap in a Tidal/Result module).
  src/tidal/render.tur: unterminated list (missing `)`); the parser
        failure is likely cascading from one of the earlier modules.

Remove this marker once `tur check src/tidal/*.tur` exits 0 for every file.
