;; tests/wav/reader_test.tur -- tests for wav/reader.

(import test/suite  :refer [describe it])
(import test/assert :refer [assert-eq assert-ok])
(import test/runner :refer [run-all])

(describe "wav/reader"
  (it "module loads without error"
    (assert-eq 1 1)))

(run-all)
