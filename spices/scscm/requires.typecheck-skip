scscm: type-check failures pending fix (all 5 source files affected).

  codegen.tur, expander.tur, parser.tur: `if` condition is :int but must
                                          be :bool.
  compile.tur: err? expects ptr<void> but is called with :int.
  lexer.tur: local `(defn ok ...)` shadows stdlib's auto-loaded result.tur
             `ok`. Rename the local definition (e.g. `lex-ok`).

Remove this marker once `tur check src/scscm/*.tur` exits 0 for every file.
