/* generated by tur (phase 2) */
#include "scscm__lexer.h"

int64_t scscm__lexer__nil_value();
bool scscm__lexer__list_nil_(int64_t);
int64_t scscm__lexer__cons(int64_t, int64_t);
int64_t scscm__lexer__head(int64_t);
int64_t scscm__lexer__tail(int64_t);
void * scscm__lexer__lex_ok(int64_t);
void * scscm__lexer__lex_err(int64_t);
bool scscm__lexer__lex_ok_(void *);
bool scscm__lexer__lex_err_(void *);
int64_t scscm__lexer__lex_ok_val(void *);
int64_t scscm__lexer__lex_err_val(void *);
static int64_t scscm__lexer__scscm_tokenize_raw(const char *);
static bool scscm__lexer__scscm_rtl_error_(int64_t);
static const char * scscm__lexer__scscm_rtl_error(int64_t);
static int64_t scscm__lexer__scscm_rtl_count(int64_t);
static int64_t scscm__lexer__scscm_rtl_get(int64_t, int64_t);
static void scscm__lexer__scscm_rtl_free(int64_t);
static void scscm__lexer__scscm_rtl_free_shallow(int64_t);
const char * scscm__lexer__token_type(int64_t);
const char * scscm__lexer__token_value(int64_t);
int64_t scscm__lexer__token_line(int64_t);
int64_t scscm__lexer__token_col(int64_t);
static int64_t scscm__lexer__scscm_tokens_build(int64_t, int64_t, int64_t);
int64_t scscm__lexer__scscm_list_rev(int64_t, int64_t);
int64_t scscm__lexer__scscm_cstr_as_int(const char *);
bool scscm__lexer__scscm_cstr_eq_(const char *, const char *);
void * scscm__lexer__tokenize(const char *);
static void scscm__lexer__scscm_token_free_one(int64_t);
void scscm__lexer__tokens_free(int64_t);
static void scscm__lexer__scscm_void_noop();

int64_t scscm__lexer__nil_value() {
        return 0;
  
}

bool scscm__lexer__list_nil_(int64_t lst) {
        return lst == 0;
  
}

int64_t scscm__lexer__cons(int64_t value, int64_t next) {
        struct { int64_t value; int64_t next; } *cell = malloc(sizeof(*cell));
  cell->value = value; cell->next = next;
  return (int64_t)(intptr_t)cell;
  
}

int64_t scscm__lexer__head(int64_t lst) {
        struct { int64_t value; int64_t next; } *cell = (void*)(intptr_t)lst;
  if (!cell) { fprintf(stderr, "head: empty list\n"); exit(1); }
  return cell->value;
  
}

int64_t scscm__lexer__tail(int64_t lst) {
        struct { int64_t value; int64_t next; } *cell = (void*)(intptr_t)lst;
  if (!cell) { fprintf(stderr, "tail: empty list\n"); exit(1); }
  return cell->next;
  
}

void * scscm__lexer__lex_ok(int64_t x) {
        struct { bool is_ok; int64_t ok_val; int64_t err_val; } *r = malloc(sizeof(*r));
  r->is_ok = true; r->ok_val = x; r->err_val = 0;
  return (void*)r;
  
}

void * scscm__lexer__lex_err(int64_t e) {
        struct { bool is_ok; int64_t ok_val; int64_t err_val; } *r = malloc(sizeof(*r));
  r->is_ok = false; r->ok_val = 0; r->err_val = e;
  return (void*)r;
  
}

bool scscm__lexer__lex_ok_(void * r) {
        struct { bool is_ok; int64_t ok_val; int64_t err_val; } *res = (void*)r;
  return res != NULL && res->is_ok;
  
}

bool scscm__lexer__lex_err_(void * r) {
        struct { bool is_ok; int64_t ok_val; int64_t err_val; } *res = (void*)r;
  return res == NULL || !res->is_ok;
  
}

int64_t scscm__lexer__lex_ok_val(void * r) {
        struct { bool is_ok; int64_t ok_val; int64_t err_val; } *res = (void*)r;
  return res->ok_val;
  
}

int64_t scscm__lexer__lex_err_val(void * r) {
        struct { bool is_ok; int64_t ok_val; int64_t err_val; } *res = (void*)r;
  return res->err_val;
  
}

static int64_t scscm__lexer__scscm_tokenize_raw(const char * source) {
        typedef struct { int type; char *value; int line; int col; } STok;
  typedef struct { int count; int cap; int64_t *handles; const char *error; } STL;
  const char *src = (const char *)(intptr_t)source;
  STL *tl = malloc(sizeof(*tl));
  tl->count = 0; tl->cap = 32; tl->error = NULL;
  tl->handles = malloc(tl->cap * sizeof(int64_t));

  int pos = 0, line = 1, col = 1;

  while (src[pos]) {
    /* skip whitespace */
    while (src[pos]==' '||src[pos]=='\t'||src[pos]=='\r'||src[pos]=='\n') {
      if (src[pos]=='\n'){line++;col=1;}else{col++;}
      pos++;
    }
    if (!src[pos]) break;
    /* skip line comments */
    if (src[pos]==';') { while(src[pos]&&src[pos]!='\n') pos++; continue; }

    int tok_line=line, tok_col=col;
    STok *tok = malloc(sizeof(*tok));
    tok->line=tok_line; tok->col=tok_col; tok->value=NULL; tok->type=-1;

    char c = src[pos];
    if      (c=='(') { tok->type=5;  tok->value=strdup("(");  pos++;col++; }
    else if (c==')') { tok->type=6;  tok->value=strdup(")");  pos++;col++; }
    else if (c=='[') { tok->type=7;  tok->value=strdup("[");  pos++;col++; }
    else if (c==']') { tok->type=8;  tok->value=strdup("]");  pos++;col++; }
    else if (c=='{') { tok->type=9;  tok->value=strdup("{");  pos++;col++; }
    else if (c=='}') { tok->type=10; tok->value=strdup("}");  pos++;col++; }
    else if (c=='\'') { tok->type=11; tok->value=strdup("'"); pos++;col++; }
    else if (c=='`') { tok->type=12; tok->value=strdup("`");  pos++;col++; }
    else if (c=='~') {
      if (src[pos+1]=='@') { tok->type=14; tok->value=strdup("~@"); pos+=2;col+=2; }
      else                 { tok->type=13; tok->value=strdup("~");  pos++;col++; }
    }
    else if (c=='#' && src[pos+1]=='(') {
      tok->type=15; tok->value=strdup("#("); pos+=2; col+=2;
    }
    else if (c=='"') {
      pos++; col++;
      int start=pos;
      while (src[pos] && src[pos]!='"') {
        if (src[pos]=='\\') { pos++;col++; if(!src[pos]) break; }
        if (src[pos]=='\n'){line++;col=1;}else{col++;}
        pos++;
      }
      if (!src[pos]) {
        tl->error = strdup("unterminated string literal");
        free(tok); return (int64_t)(intptr_t)tl;
      }
      int len=pos-start;
      tok->value=malloc(len+1); memcpy(tok->value,src+start,len); tok->value[len]='\0';
      tok->type=4; pos++;col++;
    }
    else if (c==':') {
      /* keyword :foo */
      int start=pos; pos++;col++;
      while (src[pos]&&!strchr("()[]{}\"';`,@# \t\r\n",src[pos])) { pos++;col++; }
      int len=pos-start;
      tok->value=malloc(len+1); memcpy(tok->value,src+start,len); tok->value[len]='\0';
      tok->type=2;
    }
    else if ((c>='0'&&c<='9') ||
             ((c=='-'||c=='+') && src[pos+1]>='0' && src[pos+1]<='9')) {
      int start=pos; pos++;col++;
      while (src[pos]>='0'&&src[pos]<='9') { pos++;col++; }
      if (src[pos]=='.') {
        pos++;col++;
        while (src[pos]>='0'&&src[pos]<='9') { pos++;col++; }
      }
      if (src[pos]=='e'||src[pos]=='E') {
        pos++;col++;
        if (src[pos]=='+'||src[pos]=='-') { pos++;col++; }
        while (src[pos]>='0'&&src[pos]<='9') { pos++;col++; }
      }
      int len=pos-start;
      tok->value=malloc(len+1); memcpy(tok->value,src+start,len); tok->value[len]='\0';
      tok->type=3;
    }
    else if (c && !strchr("()[]{}\"';`,@# \t\r\n",c)) {
      /* symbol or atom */
      int start=pos;
      while (src[pos]&&!strchr("()[]{}\"';`,@# \t\r\n",src[pos])) { pos++;col++; }
      int len=pos-start;
      tok->value=malloc(len+1); memcpy(tok->value,src+start,len); tok->value[len]='\0';
      if (strcmp(tok->value,"nil")==0||strcmp(tok->value,"true")==0||
          strcmp(tok->value,"false")==0)
        tok->type=0;
      else
        tok->type=1;
    }
    else {
      char msg[80];
      snprintf(msg,sizeof(msg),"unexpected char '%c' at line %d col %d",(char)c,line,col);
      tl->error=strdup(msg); free(tok); return (int64_t)(intptr_t)tl;
    }

    /* append token handle */
    if (tl->count >= tl->cap) {
      tl->cap *= 2;
      tl->handles = realloc(tl->handles, tl->cap * sizeof(int64_t));
    }
    tl->handles[tl->count++] = (int64_t)(intptr_t)tok;
  }
  return (int64_t)(intptr_t)tl;
  
}

static bool scscm__lexer__scscm_rtl_error_(int64_t tl) {
        typedef struct { int count; int cap; int64_t *handles; const char *error; } STL;
  STL *t = (STL*)(intptr_t)tl;
  return t->error != NULL;
  
}

static const char * scscm__lexer__scscm_rtl_error(int64_t tl) {
        typedef struct { int count; int cap; int64_t *handles; const char *error; } STL;
  STL *t = (STL*)(intptr_t)tl;
  return t->error ? t->error : "";
  
}

static int64_t scscm__lexer__scscm_rtl_count(int64_t tl) {
        typedef struct { int count; int cap; int64_t *handles; const char *error; } STL;
  STL *t = (STL*)(intptr_t)tl;
  return t->count;
  
}

static int64_t scscm__lexer__scscm_rtl_get(int64_t tl, int64_t i) {
        typedef struct { int count; int cap; int64_t *handles; const char *error; } STL;
  STL *t = (STL*)(intptr_t)tl;
  return t->handles[i];
  
}

static void scscm__lexer__scscm_rtl_free(int64_t tl) {
        typedef struct { int type; char *value; int line; int col; } STok;
  typedef struct { int count; int cap; int64_t *handles; const char *error; } STL;
  STL *t = (STL*)(intptr_t)tl;
  if (!t) return;
  for (int i = 0; i < t->count; i++) {
    STok *tok = (STok*)(intptr_t)t->handles[i];
    if (tok) { free(tok->value); free(tok); }
  }
  free(t->handles);
  if (t->error) free((void*)t->error);
  free(t);
  
}

static void scscm__lexer__scscm_rtl_free_shallow(int64_t tl) {
        typedef struct { int count; int cap; int64_t *handles; const char *error; } STL;
  STL *t = (STL*)(intptr_t)tl;
  if (!t) return;
  free(t->handles);
  free(t);
  
}

const char * scscm__lexer__token_type(int64_t tok) {
        typedef struct { int type; char *value; int line; int col; } STok;
  static const char *names[] = {
    ":atom",":symbol",":keyword",":number",":string",
    ":lparen",":rparen",":lbracket",":rbracket",":lbrace",":rbrace",
    ":quote",":quasiquote",":unquote",":unquote-splice",":hash-paren",":eof"
  };
  STok *t = (STok*)(intptr_t)tok;
  if (!t || t->type < 0 || t->type > 16) return ":unknown";
  return names[t->type];
  
}

const char * scscm__lexer__token_value(int64_t tok) {
        typedef struct { int type; char *value; int line; int col; } STok;
  STok *t = (STok*)(intptr_t)tok;
  return t && t->value ? t->value : "";
  
}

int64_t scscm__lexer__token_line(int64_t tok) {
        typedef struct { int type; char *value; int line; int col; } STok;
  STok *t = (STok*)(intptr_t)tok;
  return t ? t->line : 0;
  
}

int64_t scscm__lexer__token_col(int64_t tok) {
        typedef struct { int type; char *value; int line; int col; } STok;
  STok *t = (STok*)(intptr_t)tok;
  return t ? t->col : 0;
  
}

static int64_t scscm__lexer__scscm_tokens_build(int64_t tl, int64_t i, int64_t acc) {
        int64_t __t0;
        if (((i) >= (scscm__lexer__scscm_rtl_count(tl)))) {
            __t0 = acc;
        } else {
            __t0 = scscm__lexer__scscm_tokens_build(tl, ((i) + (INT64_C(1))), scscm__lexer__cons(scscm__lexer__scscm_rtl_get(tl, i), acc));
        }
        return __t0;
}

int64_t scscm__lexer__scscm_list_rev(int64_t lst, int64_t acc) {
        int64_t __t1;
        if (scscm__lexer__list_nil_(lst)) {
            __t1 = acc;
        } else {
            __t1 = scscm__lexer__scscm_list_rev(scscm__lexer__tail(lst), scscm__lexer__cons(scscm__lexer__head(lst), acc));
        }
        return __t1;
}

int64_t scscm__lexer__scscm_cstr_as_int(const char * s) {
        return (int64_t)(intptr_t)s;
  
}

bool scscm__lexer__scscm_cstr_eq_(const char * a, const char * b) {
        const char *ca = (const char*)(intptr_t)a;
  const char *cb = (const char*)(intptr_t)b;
  if (!ca || !cb) return ca == cb;
  return strcmp(ca, cb) == 0;
  
}

void * scscm__lexer__tokenize(const char * source) {
        void * __t2;
        {
            int64_t tl_63 = scscm__lexer__scscm_tokenize_raw(source);
            (void)tl_63;
            void * __t3;
            if (scscm__lexer__scscm_rtl_error_(tl_63)) {
                void * __t4;
                {
                    const char * msg_64 = scscm__lexer__scscm_rtl_error(tl_63);
                    (void)msg_64;
                    scscm__lexer__scscm_rtl_free(tl_63);
                    void * __t5;
                    __t5 = scscm__lexer__lex_err(scscm__lexer__scscm_cstr_as_int(msg_64));
                    __t4 = __t5;
                }
                __t3 = __t4;
            } else {
                void * __t6;
                {
                    int64_t reversed_65 = scscm__lexer__scscm_tokens_build(tl_63, INT64_C(0), scscm__lexer__nil_value());
                    (void)reversed_65;
                    int64_t tokens_66 = scscm__lexer__scscm_list_rev(reversed_65, scscm__lexer__nil_value());
                    (void)tokens_66;
                    scscm__lexer__scscm_rtl_free_shallow(tl_63);
                    void * __t7;
                    __t7 = scscm__lexer__lex_ok(tokens_66);
                    __t6 = __t7;
                }
                __t3 = __t6;
            }
            __t2 = __t3;
        }
        return __t2;
}

static void scscm__lexer__scscm_token_free_one(int64_t tok) {
        typedef struct { int type; char *value; int line; int col; } STok;
  STok *t = (STok*)(intptr_t)tok;
  if (t) { free(t->value); free(t); }
  
}

void scscm__lexer__tokens_free(int64_t ts) {
        if (scscm__lexer__list_nil_(ts)) {
            scscm__lexer__scscm_void_noop();
        } else {
            scscm__lexer__scscm_token_free_one(scscm__lexer__head(ts));
            scscm__lexer__tokens_free(scscm__lexer__tail(ts));
        }
}

static void scscm__lexer__scscm_void_noop() {
        (void)0;
  
}


