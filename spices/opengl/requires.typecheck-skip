opengl: type-check failures pending fix.

  src/opengl/math.tur: defn parameter count exceeds the compiler's max-8 limit
                       (likely a 4x4 matrix constructor that should take a vec
                       or a struct instead).

Remove this marker once `tur check src/opengl/*.tur` exits 0 for every file.
