/* generated by tur (phase 2) */
#include "raygui__layout.h"

int64_t raygui__layout__gui_hywindow_hybox(Rect, const char *);
void raygui__layout__gui_hygroup_hybox(Rect, const char *);
void raygui__layout__gui_hyline(Rect, const char *);
void raygui__layout__gui_hypanel(Rect, const char *);
int64_t raygui__layout__gui_hytab_hybar(Rect, const char *, int64_t, int64_t);
int64_t raygui__layout__gui_hyscroll_hypanel(Rect, const char *, Rect, int64_t);
void raygui__layout__gui_hydummy_hyrec(Rect, const char *);
void raygui__layout__gui_hystatus_hybar(Rect, const char *);

int64_t raygui__layout__gui_hywindow_hybox(Rect r, const char * title) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  return (int64_t)GuiWindowBox(bounds, (const char *)title);
  
}

void raygui__layout__gui_hygroup_hybox(Rect r, const char * text) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  GuiGroupBox(bounds, (const char *)text);
  
}

void raygui__layout__gui_hyline(Rect r, const char * text) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  GuiLine(bounds, (const char *)text);
  
}

void raygui__layout__gui_hypanel(Rect r, const char * text) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  GuiPanel(bounds, (const char *)text);
  
}

int64_t raygui__layout__gui_hytab_hybar(Rect r, const char * text, int64_t count, int64_t active) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  int a = (int)active;
  GuiTabBar(bounds, (const char **)text, (int)count, &a);
  return (int64_t)a;
  
}

int64_t raygui__layout__gui_hyscroll_hypanel(Rect r, const char * text, Rect content, int64_t scroll) {
        #include <raylib.h>
  #include <raygui.h>
  #include <string.h>
  Rectangle bounds  = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  Rectangle content_r = { (float)content.x, (float)content.y,
                           (float)content.width, (float)content.height };
  /* Unpack scroll from two packed floats in a single int64. */
  Vector2 sv = { 0.0f, 0.0f };
  if (scroll != 0) {
      uint32_t lx, ly;
      lx = (uint32_t)( scroll        & 0xFFFFFFFF);
      ly = (uint32_t)((scroll >> 32) & 0xFFFFFFFF);
      memcpy(&sv.x, &lx, sizeof(float));
      memcpy(&sv.y, &ly, sizeof(float));
  }
  Rectangle view_r = { 0 };
  GuiScrollPanel(bounds, (const char *)text, content_r, &sv, &view_r);
  /* Pack result back. */
  uint32_t ox, oy;
  memcpy(&ox, &sv.x, sizeof(float));
  memcpy(&oy, &sv.y, sizeof(float));
  return (int64_t)(((int64_t)oy << 32) | (int64_t)ox);
  
}

void raygui__layout__gui_hydummy_hyrec(Rect r, const char * text) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  GuiDummyRec(bounds, (const char *)text);
  
}

void raygui__layout__gui_hystatus_hybar(Rect r, const char * text) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  GuiStatusBar(bounds, (const char *)text);
  
}


