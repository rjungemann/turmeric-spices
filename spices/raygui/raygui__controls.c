/* generated by tur (phase 2) */
#include "raygui__controls.h"

int64_t raygui__controls__make_hytext_hybuf(int64_t);
void raygui__controls__free_hytext_hybuf(int64_t);
const char * raygui__controls__text_hybuf_hy_gtcstr(int64_t);
void raygui__controls__gui_hylabel(Rect, const char *);
int64_t raygui__controls__gui_hybutton(Rect, const char *);
int64_t raygui__controls__gui_hylabel_hybutton(Rect, const char *);
int64_t raygui__controls__gui_hytoggle(Rect, const char *, int64_t);
int64_t raygui__controls__gui_hytoggle_hygroup(Rect, const char *, int64_t);
int64_t raygui__controls__gui_hycheck_hybox(Rect, const char *, int64_t);
double raygui__controls__gui_hyslider(Rect, const char *, const char *, double, double, double);
double raygui__controls__gui_hyslider_hybar(Rect, const char *, const char *, double, double, double);
double raygui__controls__gui_hyprogress_hybar(Rect, const char *, const char *, double, double, double);
int64_t raygui__controls__gui_hytext_hybox(Rect, int64_t, int64_t, int64_t);
int64_t raygui__controls__gui_hytext_hybox_hymulti(Rect, int64_t, int64_t, int64_t);
int64_t raygui__controls__gui_hyspinner(Rect, const char *, int64_t, int64_t, int64_t, int64_t);
int64_t raygui__controls__gui_hyvalue_hybox(Rect, const char *, int64_t, int64_t, int64_t, int64_t);
int64_t raygui__controls__gui_hycombo_hybox(Rect, const char *, int64_t);
int64_t raygui__controls__gui_hydropdown_hybox(Rect, const char *, int64_t, int64_t);
int64_t raygui__controls__gui_hylist_hyview(Rect, const char *, int64_t, int64_t);
int64_t raygui__controls__gui_hylist_hyview_hyex(Rect, const char *, int64_t, int64_t, int64_t, int64_t);
int64_t raygui__controls__gui_hycolor_hypicker(Rect, const char *, int64_t);
double raygui__controls__gui_hycolor_hybar_hyalpha(Rect, const char *, double);
double raygui__controls__gui_hycolor_hybar_hyhue(Rect, const char *, double);
int64_t raygui__controls__gui_hymessage_hybox(Rect, const char *, const char *, const char *);
int64_t raygui__controls__gui_hytext_hyinput_hybox(Rect, const char *, const char *, const char *, int64_t, int64_t, int64_t);
int64_t raygui__controls__gui_hygrid(Rect, const char *, double, int64_t);

int64_t raygui__controls__make_hytext_hybuf(int64_t capacity) {
        #include <stdlib.h>
  char *buf = (char *)calloc((size_t)capacity, 1);
  return (int64_t)(intptr_t)buf;
  
}

void raygui__controls__free_hytext_hybuf(int64_t buf) {
        free((void *)(intptr_t)buf);
  
}

const char * raygui__controls__text_hybuf_hy_gtcstr(int64_t buf) {
        return (int64_t)(intptr_t)(const char *)(intptr_t)buf;
  
}

void raygui__controls__gui_hylabel(Rect r, const char * text) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  GuiLabel(bounds, (const char *)text);
  
}

int64_t raygui__controls__gui_hybutton(Rect r, const char * text) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  return (int64_t)GuiButton(bounds, (const char *)text);
  
}

int64_t raygui__controls__gui_hylabel_hybutton(Rect r, const char * text) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  return (int64_t)GuiLabelButton(bounds, (const char *)text);
  
}

int64_t raygui__controls__gui_hytoggle(Rect r, const char * text, int64_t active) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  bool a = (bool)active;
  GuiToggle(bounds, (const char *)text, &a);
  return (int64_t)a;
  
}

int64_t raygui__controls__gui_hytoggle_hygroup(Rect r, const char * text, int64_t active) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  int a = (int)active;
  GuiToggleGroup(bounds, (const char *)text, &a);
  return (int64_t)a;
  
}

int64_t raygui__controls__gui_hycheck_hybox(Rect r, const char * text, int64_t checked) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  bool c = (bool)checked;
  GuiCheckBox(bounds, (const char *)text, &c);
  return (int64_t)c;
  
}

double raygui__controls__gui_hyslider(Rect r, const char * lo, const char * hi, double val, double min, double max) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  float v = (float)val;
  GuiSlider(bounds, (const char *)lo, (const char *)hi, &v, (float)min, (float)max);
  return (double)v;
  
}

double raygui__controls__gui_hyslider_hybar(Rect r, const char * lo, const char * hi, double val, double min, double max) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  float v = (float)val;
  GuiSliderBar(bounds, (const char *)lo, (const char *)hi, &v, (float)min, (float)max);
  return (double)v;
  
}

double raygui__controls__gui_hyprogress_hybar(Rect r, const char * lo, const char * hi, double val, double min, double max) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  float v = (float)val;
  GuiProgressBar(bounds, (const char *)lo, (const char *)hi, &v, (float)min, (float)max);
  return (double)v;
  
}

int64_t raygui__controls__gui_hytext_hybox(Rect r, int64_t buf, int64_t size, int64_t edit) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  bool edit_b = (bool)edit;
  int result = GuiTextBox(bounds, (char *)(intptr_t)buf, (int)size, edit_b);
  if (result) edit_b = !edit_b;
  return (int64_t)edit_b;
  
}

int64_t raygui__controls__gui_hytext_hybox_hymulti(Rect r, int64_t buf, int64_t size, int64_t edit) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  bool edit_b = (bool)edit;
  int result = GuiTextBoxMulti(bounds, (char *)(intptr_t)buf, (int)size, edit_b);
  if (result) edit_b = !edit_b;
  return (int64_t)edit_b;
  
}

int64_t raygui__controls__gui_hyspinner(Rect r, const char * text, int64_t val, int64_t min, int64_t max, int64_t edit) {
        #include <raylib.h>
  #include <raygui.h>
  #include <stdlib.h>
  typedef struct { int64_t head; int64_t tail; } __tur_pair_cell;
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  bool edit_b = (bool)edit;
  int v = (int)val;
  int result = GuiSpinner(bounds, (const char *)text, &v, (int)min, (int)max, edit_b);
  if (result) edit_b = !edit_b;
  __tur_pair_cell *p = (__tur_pair_cell *)malloc(sizeof(__tur_pair_cell));
  p->head = (int64_t)edit_b;
  p->tail = (int64_t)v;
  return (int64_t)(intptr_t)p;
  
}

int64_t raygui__controls__gui_hyvalue_hybox(Rect r, const char * text, int64_t val, int64_t min, int64_t max, int64_t edit) {
        #include <raylib.h>
  #include <raygui.h>
  #include <stdlib.h>
  typedef struct { int64_t head; int64_t tail; } __tur_pair_cell;
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  bool edit_b = (bool)edit;
  int v = (int)val;
  int result = GuiValueBox(bounds, (const char *)text, &v, (int)min, (int)max, edit_b);
  if (result) edit_b = !edit_b;
  __tur_pair_cell *p = (__tur_pair_cell *)malloc(sizeof(__tur_pair_cell));
  p->head = (int64_t)edit_b;
  p->tail = (int64_t)v;
  return (int64_t)(intptr_t)p;
  
}

int64_t raygui__controls__gui_hycombo_hybox(Rect r, const char * text, int64_t active) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  int a = (int)active;
  GuiComboBox(bounds, (const char *)text, &a);
  return (int64_t)a;
  
}

int64_t raygui__controls__gui_hydropdown_hybox(Rect r, const char * text, int64_t active, int64_t edit) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  int a = (int)active;
  bool edit_b = (bool)edit;
  int result = GuiDropdownBox(bounds, (const char *)text, &a, edit_b);
  if (result) edit_b = !edit_b;
  return (int64_t)edit_b;
  
}

int64_t raygui__controls__gui_hylist_hyview(Rect r, const char * text, int64_t scroll, int64_t active) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  int s = (int)scroll;
  int a = (int)active;
  GuiListView(bounds, (const char *)text, &s, &a);
  return (int64_t)a;
  
}

int64_t raygui__controls__gui_hylist_hyview_hyex(Rect r, const char * text, int64_t count, int64_t scroll, int64_t active, int64_t focus) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  int s = (int)scroll;
  int a = (int)active;
  int f = (int)focus;
  GuiListViewEx(bounds, (const char **)text, (int)count, &s, &a, &f);
  return (int64_t)a;
  
}

int64_t raygui__controls__gui_hycolor_hypicker(Rect r, const char * text, int64_t color) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  Color c;
  c.r = (unsigned char)((color >> 24) & 0xFF);
  c.g = (unsigned char)((color >> 16) & 0xFF);
  c.b = (unsigned char)((color >>  8) & 0xFF);
  c.a = (unsigned char)( color        & 0xFF);
  GuiColorPicker(bounds, (const char *)text, &c);
  return (int64_t)(((int64_t)c.r << 24) | ((int64_t)c.g << 16) |
                   ((int64_t)c.b <<  8) |  (int64_t)c.a);
  
}

double raygui__controls__gui_hycolor_hybar_hyalpha(Rect r, const char * text, double alpha) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  float a = (float)alpha;
  GuiColorBarAlpha(bounds, (const char *)text, &a);
  return (double)a;
  
}

double raygui__controls__gui_hycolor_hybar_hyhue(Rect r, const char * text, double value) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  float v = (float)value;
  GuiColorBarHue(bounds, (const char *)text, &v);
  return (double)v;
  
}

int64_t raygui__controls__gui_hymessage_hybox(Rect r, const char * title, const char * message, const char * buttons) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  return (int64_t)GuiMessageBox(bounds, (const char *)title,
                                (const char *)message, (const char *)buttons);
  
}

int64_t raygui__controls__gui_hytext_hyinput_hybox(Rect r, const char * title, const char * message, const char * buttons, int64_t buf, int64_t size, int64_t secret) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  bool sec = (bool)secret;
  return (int64_t)GuiTextInputBox(bounds, (const char *)title,
                                  (const char *)message, (const char *)buttons,
                                  (char *)(intptr_t)buf, (int)size, &sec);
  
}

int64_t raygui__controls__gui_hygrid(Rect r, const char * text, double spacing, int64_t divs) {
        #include <raylib.h>
  #include <raygui.h>
  Rectangle bounds = { (float)r.x, (float)r.y, (float)r.width, (float)r.height };
  Vector2 cell = GuiGrid(bounds, (const char *)text, (float)spacing, (int)divs, NULL);
  if (cell.x < 0.0f) return (int64_t)-1;
  return (int64_t)(((int64_t)(int)cell.y << 32) | (int64_t)(int)cell.x);
  
}


