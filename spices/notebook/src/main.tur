;;; main -- tur-notebook CLI entrypoint.
;; main -- dispatch to notebook/cli using *args*.

(defmodule main
  (import notebook/cli :refer [cli-main])
  (export main)

  (defn main [] #{Unsafe} : int
    (cli-main *args*))

) ;; end defmodule main
