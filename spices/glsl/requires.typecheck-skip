glsl: type-check failures pending fix.

  src/glsl/shaders.tur: __prim-keyword expects :int but is called with :cstr.
  src/glsl/stdlib.tur:  unbound symbol '.' (the `dot` macro from stdlib/macros.tur
                        appears unsupported in this context).

Remove this marker once `tur check src/glsl/*.tur` exits 0 for every file.
