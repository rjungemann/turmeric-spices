/* generated by tur (phase 2) */
#include "osc__server.h"

static void * osc__server____ok(int64_t);
static void * osc__server____err(int64_t);
void * osc__server__server_hynew(const char *, const char *);
void * osc__server__server_hynew_hytcp(const char *);
void osc__server__server_hyfree(int64_t);
void osc__server__server_hyadd_hymethod(int64_t, const char *, const char *, int64_t);
int64_t osc__server__server_hyrecv(int64_t, int64_t);
int64_t osc__server__server_hyrecv_hynoblock(int64_t);

static void * osc__server____ok(int64_t v) {
        #include <stdlib.h>
  struct { bool is_ok; int64_t ok_val; int64_t err_val; } *r = malloc(sizeof(*r));
  r->is_ok = true; r->ok_val = v; r->err_val = 0; return r;
  
}

static void * osc__server____err(int64_t e) {
        #include <stdlib.h>
  struct { bool is_ok; int64_t ok_val; int64_t err_val; } *r = malloc(sizeof(*r));
  r->is_ok = false; r->ok_val = 0; r->err_val = e; return r;
  
}

void * osc__server__server_hynew(const char * port, const char * proto) {
        #include <stdlib.h>
  #include <lo/lo.h>
  (void)proto; /* proto arg reserved; always UDP in this implementation */
  lo_server_thread st = lo_server_thread_new((const char *)port, NULL);
  struct { bool is_ok; int64_t ok_val; int64_t err_val; } *r = malloc(sizeof(*r));
  if (!st) {
    r->is_ok = false; r->ok_val = 0; r->err_val = 0;
  } else {
    lo_server_thread_start(st);
    r->is_ok = true; r->ok_val = (int64_t)(intptr_t)st; r->err_val = 0;
  }
  return r;
  
}

void * osc__server__server_hynew_hytcp(const char * port) {
        #include <stdlib.h>
  #include <lo/lo.h>
  lo_server_thread st = lo_server_thread_new_with_proto((const char *)port,
                                                        LO_TCP, NULL);
  struct { bool is_ok; int64_t ok_val; int64_t err_val; } *r = malloc(sizeof(*r));
  if (!st) {
    r->is_ok = false; r->ok_val = 0; r->err_val = 0;
  } else {
    lo_server_thread_start(st);
    r->is_ok = true; r->ok_val = (int64_t)(intptr_t)st; r->err_val = 0;
  }
  return r;
  
}

void osc__server__server_hyfree(int64_t s) {
        #include <lo/lo.h>
  lo_server_thread st = (lo_server_thread)(intptr_t)s;
  lo_server_thread_stop(st);
  lo_server_thread_free(st);
  
}

void osc__server__server_hyadd_hymethod(int64_t s, const char * path, const char * typespec, int64_t handler) {
        #include <lo/lo.h>
  /* Stub trampoline -- full Turmeric FFI callback support is deferred.
     A no-op handler is registered so the method entry exists in liblo's
     dispatch table.  Replace handler_stub with a real trampoline once
     the Turmeric callback API is available. */
  static int handler_stub(const char *p, const char *t,
                          lo_arg **argv, int argc,
                          lo_message msg, void *data) {
    (void)p; (void)t; (void)argv; (void)argc; (void)msg; (void)data;
    return 0;
  }
  lo_server_thread st = (lo_server_thread)(intptr_t)s;
  lo_server_thread_add_method(st,
    path ? (const char *)path : NULL,
    typespec ? (const char *)typespec : NULL,
    handler_stub, NULL);
  
}

int64_t osc__server__server_hyrecv(int64_t s, int64_t timeout_ms) {
        #include <lo/lo.h>
  lo_server_thread st = (lo_server_thread)(intptr_t)s;
  lo_server srv = lo_server_thread_get_server(st);
  return (int64_t)lo_server_recv_noblock(srv, (int)timeout_ms);
  
}

int64_t osc__server__server_hyrecv_hynoblock(int64_t s) {
        #include <lo/lo.h>
  lo_server_thread st = (lo_server_thread)(intptr_t)s;
  lo_server srv = lo_server_thread_get_server(st);
  return (int64_t)lo_server_recv_noblock(srv, 0);
  
}


