;; tests/osc/msg_test.tur -- tests for osc/msg.

(import test/suite  :refer [describe it])
(import test/assert :refer [assert-eq])
(import test/runner :refer [run-all])

(describe "osc/msg"
  (it "module loads without error"
    (assert-eq 1 1)))

(run-all)
