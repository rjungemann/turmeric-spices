/* generated by tur (phase 2) */
#include "raylib__text.h"

int64_t raylib__text__load_hyfont(const char *);
void raylib__text__unload_hyfont(int64_t);
void raylib__text__draw_hytext(const char *, int64_t, int64_t, int64_t, int64_t);
void raylib__text__draw_hytext_hyex(int64_t, const char *, double, double, double, double, int64_t);
int64_t raylib__text__measure_hytext(const char *, int64_t);

int64_t raylib__text__load_hyfont(const char * path) {
        #include <raylib.h>
  Font *f = malloc(sizeof(Font));
  *f = LoadFont((const char *)path);
  return (int64_t)(intptr_t)f;
  
}

void raylib__text__unload_hyfont(int64_t font) {
        #include <raylib.h>
  Font *f = (Font *)(intptr_t)font;
  UnloadFont(*f);
  free(f);
  
}

void raylib__text__draw_hytext(const char * text, int64_t x, int64_t y, int64_t size, int64_t col) {
        #include <raylib.h>
  Color *c = (Color *)(intptr_t)col;
  DrawText((const char *)text, (int)x, (int)y, (int)size, *c);
  
}

void raylib__text__draw_hytext_hyex(int64_t font, const char * text, double px, double py, double size, double spacing, int64_t col) {
        #include <raylib.h>
  Font  *f = (Font  *)(intptr_t)font;
  Color *c = (Color *)(intptr_t)col;
  Vector2 pos = { (float)px, (float)py };
  DrawTextEx(*f, (const char *)text, pos, (float)size, (float)spacing, *c);
  
}

int64_t raylib__text__measure_hytext(const char * text, int64_t size) {
        #include <raylib.h>
  return (int64_t)MeasureText((const char *)text, (int)size);
  
}


