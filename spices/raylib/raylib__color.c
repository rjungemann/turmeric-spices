/* generated by tur (phase 2) */
#include "raylib__color.h"

int64_t raylib__color__color(int64_t, int64_t, int64_t, int64_t);
int64_t raylib__color__red();
int64_t raylib__color__green();
int64_t raylib__color__blue();
int64_t raylib__color__white();
int64_t raylib__color__black();
int64_t raylib__color__gray();
int64_t raylib__color__yellow();
int64_t raylib__color__orange();
int64_t raylib__color__purple();
int64_t raylib__color__pink();
int64_t raylib__color__skyblue();
int64_t raylib__color__raywhite();
int64_t raylib__color__fade(int64_t, double);
int64_t raylib__color__color_hylerp(int64_t, int64_t, double);

int64_t raylib__color__color(int64_t r, int64_t g, int64_t b, int64_t a) {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color));
  c->r = (unsigned char)r;
  c->g = (unsigned char)g;
  c->b = (unsigned char)b;
  c->a = (unsigned char)a;
  return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__red() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = RED; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__green() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = GREEN; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__blue() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = BLUE; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__white() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = WHITE; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__black() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = BLACK; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__gray() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = GRAY; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__yellow() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = YELLOW; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__orange() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = ORANGE; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__purple() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = PURPLE; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__pink() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = PINK; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__skyblue() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = SKYBLUE; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__raywhite() {
        #include <raylib.h>
  Color *c = malloc(sizeof(Color)); *c = RAYWHITE; return (int64_t)(intptr_t)c;
  
}

int64_t raylib__color__fade(int64_t col, double alpha) {
        #include <raylib.h>
  Color *src = (Color *)(intptr_t)col;
  Color *out = malloc(sizeof(Color));
  *out = ColorAlpha(*src, (float)alpha);
  return (int64_t)(intptr_t)out;
  
}

int64_t raylib__color__color_hylerp(int64_t c1, int64_t c2, double t) {
        #include <raylib.h>
  Color *a = (Color *)(intptr_t)c1;
  Color *b = (Color *)(intptr_t)c2;
  Color *out = malloc(sizeof(Color));
  *out = ColorLerp(*a, *b, (float)t);
  return (int64_t)(intptr_t)out;
  
}


