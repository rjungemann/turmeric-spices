raylib: type-check failures pending fix.

  src/raylib/camera.tur, models.tur: defn parameter count exceeds the
                                      compiler's max-8 limit. Likely needs
                                      a struct (Vector3, Camera3D etc.)
                                      instead of flat float lists.

Remove this marker once `tur check src/raylib/*.tur` exits 0 for every file.
