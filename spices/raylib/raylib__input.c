/* generated by tur (phase 2) */
#include "raylib__input.h"

bool raylib__input__is_hykey_hydown(int64_t);
bool raylib__input__is_hykey_hypressed(int64_t);
bool raylib__input__is_hykey_hyreleased(int64_t);
bool raylib__input__is_hymouse_hybutton_hydown(int64_t);
bool raylib__input__is_hymouse_hybutton_hypressed(int64_t);
int64_t raylib__input__get_hymouse_hyposition();
int64_t raylib__input__get_hymouse_hydelta();
double raylib__input__get_hymouse_hywheel_hymove();

bool raylib__input__is_hykey_hydown(int64_t key) {
        #include <raylib.h>
  return IsKeyDown((int)key);
  
}

bool raylib__input__is_hykey_hypressed(int64_t key) {
        #include <raylib.h>
  return IsKeyPressed((int)key);
  
}

bool raylib__input__is_hykey_hyreleased(int64_t key) {
        #include <raylib.h>
  return IsKeyReleased((int)key);
  
}

bool raylib__input__is_hymouse_hybutton_hydown(int64_t button) {
        #include <raylib.h>
  return IsMouseButtonDown((int)button);
  
}

bool raylib__input__is_hymouse_hybutton_hypressed(int64_t button) {
        #include <raylib.h>
  return IsMouseButtonPressed((int)button);
  
}

int64_t raylib__input__get_hymouse_hyposition() {
        #include <raylib.h>
  Vector2 *v = malloc(sizeof(Vector2));
  *v = GetMousePosition();
  return (int64_t)(intptr_t)v;
  
}

int64_t raylib__input__get_hymouse_hydelta() {
        #include <raylib.h>
  Vector2 *v = malloc(sizeof(Vector2));
  *v = GetMouseDelta();
  return (int64_t)(intptr_t)v;
  
}

double raylib__input__get_hymouse_hywheel_hymove() {
        #include <raylib.h>
  return (double)GetMouseWheelMove();
  
}


