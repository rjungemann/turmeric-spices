/* generated by tur (phase 2) */
#include "raylib__camera.h"

int64_t raylib__camera__camera_hy3d(double, double, double, double, double, double, double, double, double, double, int64_t);
int64_t raylib__camera__camera_hy2d(double, double, double, double, double, double);
void raylib__camera__update_hycamera(int64_t, int64_t);

int64_t raylib__camera__camera_hy3d(double px, double py, double pz, double tx, double ty, double tz, double ux, double uy, double uz, double fovy, int64_t proj) {
        #include <raylib.h>
  Camera3D *cam = malloc(sizeof(Camera3D));
  cam->position   = (Vector3){ (float)px, (float)py, (float)pz };
  cam->target     = (Vector3){ (float)tx, (float)ty, (float)tz };
  cam->up         = (Vector3){ (float)ux, (float)uy, (float)uz };
  cam->fovy       = (float)fovy;
  cam->projection = (int)proj;
  return (int64_t)(intptr_t)cam;
  
}

int64_t raylib__camera__camera_hy2d(double ox, double oy, double tx, double ty, double rotation, double zoom) {
        #include <raylib.h>
  Camera2D *cam = malloc(sizeof(Camera2D));
  cam->offset   = (Vector2){ (float)ox, (float)oy };
  cam->target   = (Vector2){ (float)tx, (float)ty };
  cam->rotation = (float)rotation;
  cam->zoom     = (float)zoom;
  return (int64_t)(intptr_t)cam;
  
}

void raylib__camera__update_hycamera(int64_t camera, int64_t mode) {
        #include <raylib.h>
  Camera3D *cam = (Camera3D *)(intptr_t)camera;
  UpdateCamera(cam, (int)mode);
  
}


