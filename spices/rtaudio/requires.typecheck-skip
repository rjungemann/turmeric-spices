rtaudio: type-check failures pending fix.

  src/rtaudio/stream.tur: defn parameter count exceeds the compiler's
                          max-8 limit (audio callback signature).

Remove this marker once `tur check src/rtaudio/*.tur` exits 0 for every file.
